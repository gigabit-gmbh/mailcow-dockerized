<!DOCTYPE html>
<html lang="{{ session.mailcow_locale }}">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>mailcow UI</title>
    <!--[if lt IE 9]>
    <script src="/js/html5shiv.min.js"></script>
    <script src="/js/respond.min.js"></script>
    <![endif]-->
    <script src="/js/jquery-1.12.4.min.js"></script>
    <?php if (strtolower(trim($DEFAULT_THEME)) != "lumen"): ?>
    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/bootswatch/3.3.7/{{ default_theme|trim()|lower }}/bootstrap.min.css">
    <?php else: ?>
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <?php endif; ?>
    <link rel="stylesheet" href="/css/bootstrap-select.min.css">
    <link rel="stylesheet" href="/css/bootstrap-slider.min.css">
    <link rel="stylesheet" href="/css/bootstrap-switch.min.css">
    <link rel="stylesheet" href="/css/footable.bootstrap.min.css">
    <link rel="stylesheet" href="/inc/languages.min.css">
    <link rel="stylesheet" href="/css/mailcow.css">
    <link rel="stylesheet" href="/css/animate.min.css">
    {% if 'mailbox.php' in requestURI %}<link rel="stylesheet" href="/css/mailbox.css">{% endif %}
    {% if 'admin.php' in requestURI %} <link rel="stylesheet" href="/css/admin.css">{% endif %}
    {% if 'user.php' in requestURI %} <link rel="stylesheet" href="/css/admin.css">{% endif %}
    {% if 'edit.php' in requestURI %} <link rel="stylesheet" href="/css/edit.css">{% endif %}
    <link rel="shortcut icon" href="/favicon.png" type="image/png">
    <link rel="icon" href="/favicon.png" type="image/png">
</head>
<body style="padding-top: 70px;" id="top">
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="/"><img alt="mailcow-logo" src="{{ mainLogo|default('/img/cow_mailcow.svg') }}"></a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                {% if session.mailcow_locale is defined %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="lang-sm lang-lbl" lang="{{ session.mailcow_locale }}"></span><span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            {% for language in availableLanguages %}
                                {% set languageParameters = getRequest|merge({ 'lang': language }) %}
                                <li{% if session.mailcow_locale == language %} class="active"{% endif %}><a href="{{ languageParameters|url_encode }}"><span class="lang-xs lang-lbl-full" lang="{{ language }}"></span></a></li>
                            {% endfor %}
                        </ul>
                    </li>
                {% endif %}
                {% if session.mailcow_cc_role is defined %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">{{ lang.header.mailcow_settings }} <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        {% if session.mailcow_cc_role == 'admin' %}
                            <li{% if 'admin' in requestURI %} class="active"{% endif %}><a href="/admin.php">{{ lang.header.administration }}</a></li>
                        {% endif %}
                        {% if (session.mailcow_cc_role == 'admin') or (session.mailcow_cc_role == 'domainadmin') %}
                            <li{% if 'mailbox' in requestURI %} class="active"{% endif %}><a href="/mailbox.php">{{ lang.header.mailboxes }}</a></li>
                        {% endif %}
                        {% if session.mailcow_cc_role == 'admin'%}
                            <li{% if 'user' in requestURI %} class="active"{% endif %}><a href="/mailbox.php">{{ lang.header.user_settings }}</a></li>
                        {% endif %}
                    </ul>
                </li>
                {% if session.mailcow_cc_role == 'admin' %}
                    <li><a href data-toggle="modal" data-target="#RestartSOGo"><span style="font-size: 12px;" class="glyphicon glyphicon-refresh" aria-hidden="true"></span> {{ lang.header.restart_sogo }}</a></li>
                {% endif %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="glyphicon glyphicon-link" aria-hidden="true"></span> Apps <span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        {% for app in apps %}
                            <li title="{{ app.description|escape }}"><a href="{{ app.link|escape }}">{{ app.name|escape }}</a></li>
                        {% endfor %}
                        {% for appLink in appLinks %}
                            {% for key, value in appLink %}
                                <li><a href="{{ val|escape }}">{{ key|escape}}</a></li>
                            {% endfor %}
                        {% endfor %}
                    </ul>
                </li>
                {% endif %}
                {% if (session.dual-login is not defined) and session.mailcow_cc_username is defined %}
                    <li><a href="#" style="border-left: 1px solid #E7E7E7;" onclick="logout.submit()">{{ lang.header.logged_in_as_logout|format(session.mailcow_cc_username)|raw }}</a></li>
                {% elseif session.dual-login is defined %}
                    <li><a href="#" style="border-left: 1px solid #E7E7E7;" onclick="logout.submit()">{{ lang.header.logged_in_as_logout_dual|format(session.mailcow_cc_username, session.dual-login.username)|raw }}</a></li>
                {% endif %}
            </ul>
        </div><!--/.nav-collapse -->
    </div><!--/.container-fluid -->
</nav>
<form action="/" method="post" id="logout"><input type="hidden" name="logout"></form>